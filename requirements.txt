PySide6>=6.6
PyMuPDF>=1.25
openpyxl>=3.1
python-docx>=1.1
pdf2docx>=0.5.8
pytesseract>=0.3.10
Pillow>=10.0
anthropic>=0.40
# Design rule checking. PyDRC is a sibling project rather than a PyPI
# package; the audit reports itself unavailable when it is absent, so the
# app still runs without it.
PyYAML>=6.0
pydrc @ git+https://github.com/MoogMan1073/PyDRC@main
